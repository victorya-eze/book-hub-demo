# BookReview Hub Frontend

This Next.js app consumes the Flask backend API. Copy `.env.example` to `.env.local` and adjust `NEXT_PUBLIC_API_URL` if needed.

Install dependencies with `pnpm install` then run `pnpm dev`.
